# Core dependencies
requests>=2.28.0,<3.0.0
beautifulsoup4>=4.11.0,<5.0.0
selenium>=4.8.0,<5.0.0

# HTML/XML parsing
lxml>=4.9.0,<5.0.0
html5lib>=1.1,<2.0.0

# Web driver management
webdriver-manager>=3.8.0,<4.0.0

# Utility libraries
python-dotenv>=1.0.0,<2.0.0
tqdm>=4.65.0,<5.0.0  # For progress bars
colorama>=0.4.6,<1.0.0  # For colored terminal output
retry>=0.9.2,<1.0.0  # For automatic retrying
pyopenssl>=23.0.0,<24.0.0  # For SSL support
cryptography>=40.0.0,<41.0.0  # For secure storage of cookies

# Date and time handling
pytz>=2023.3
python-dateutil>=2.8.2,<3.0.0

# Data processing and export
pandas>=2.0.0,<3.0.0  # Optional, for advanced data manipulation
openpyxl>=3.1.0,<4.0.0  # Optional, for Excel export support

# Proxy support
pysocks>=1.7.1,<2.0.0  # Optional, for SOCKS proxy support

# HTTP Caching
requests-cache>=1.0.0,<2.0.0  # Optional, for response caching

# User agent management
fake-useragent>=1.1.0,<2.0.0  # Optional, for more user agents

# Rate limiting
ratelimit>=2.2.1,<3.0.0  # Optional, for advanced rate limiting

# Testing
pytest>=7.3.1,<8.0.0  # Optional, for testing
pytest-cov>=4.1.0,<5.0.0  # Optional, for test coverage

